# ORILIB - QUICK REFERENCE
## Ultra-Fast Lookup

**Module**: `orilib.py` (Extended Version)  
**Functions**: 43 | **Type**: Condensed Reference

---

## ⚡ FUNCTION LIST

**`QMatproduct`** - Multiply a single symmetry quaternion with multiple quaterni

**`Qlog`** - Compute the logarithm of quaternions (quaternion logarithm m

**`Qproduct`** - Compute quaternion product for arrays of quaternions.

**`active_rotation`** - Perform active rotation of vector v by rotation matrix g.

**`eu2quat`** - Convert Bunge Euler angles to quaternion representation.

**`euler_angles_reduction`** - Reduce Euler angles to fundamental zone using symmetry opera

**`grid_s1`** - Generate uniformly distributed points on S¹ (circle).

**`hopf2quat`** - Convert Hopf coordinates to quaternions.

**`mat_to_quat`** - Convert a rotation matrix to a quaternion representation.

**`misori_sym_deg_quats`** - Calculate minimum misorientation angle between two quaternio

**`misori_sym_deg_sample_to_crystal_fast`** - Compute misorientation angles (deg) between orientations M1 

**`mk_pix2xy`** - Create lookup tables for HEALPix pixel index to (x,y) coordi

**`np_euler_matrix`** - Convert Euler angles to rotation matrix (ZXZ convention, Bun

**`np_eulers_matrices`** - Convert multiple Euler angles to rotation matrices (vectoriz

**`np_g2quats`** - Convert multiple rotation matrices to quaternions (vectorize

**`np_gmat2rodrigues`** - Convert rotation matrix to Rodrigues-Frank vector representa

**`np_inverse_euler_matrix`** - Convert Euler angles to inverse (transpose) rotation matrix.

**`np_ol_R_g`** - Convert Rodrigues-Frank vector to rotation matrix (NumPy ver

**`np_ol_R_g2`** - Alternative Rodrigues-Frank to rotation matrix (NumPy versio

**`np_ol_R_q2`** - Convert Rodrigues-Frank vector to quaternion (method 2).

**`np_ol_g_R`** - Convert rotation matrix to Rodrigues-Frank vector (NumPy ver

**`np_ol_g_R2`** - Alternative Rodrigues-Frank conversion (NumPy version, metho

**`np_ol_g_q2`** - Convert rotation matrix to quaternion (method 2).

**`np_ol_g_rtheta_rad`** - Convert rotation matrix to axis-angle representation (NumPy 

**`np_ol_q_g`** - Convert quaternion to rotation matrix.

**`np_ol_rtheta_g_rad`** - Convert axis-angle representation to rotation matrix (NumPy 

**`np_rodrigues2gmat`** - Convert Rodrigues-Frank vector to rotation matrix.

**`nside2npix`** - Calculate the number of pixels in a HEALPix map.

**`ol_R_g`** - Convert Rodrigues-Frank vector to rotation matrix (list vers

**`ol_R_g2`** - Alternative Rodrigues-Frank to rotation matrix (list version

**`ol_g_R`** - Convert rotation matrix to Rodrigues-Frank vector (list vers

**`ol_g_R2`** - Alternative Rodrigues-Frank conversion (list version, method

**`ol_g_rtheta_rad`** - Convert rotation matrix to axis-angle representation (Rodrig

**`ol_rtheta_g_rad`** - Convert axis-angle representation to rotation matrix using R

**`passive_rotation`** - Perform passive rotation (coordinate transformation).

**`pix2ang_nest`** - Convert HEALPix pixel index to spherical coordinates (theta,

**`quat_conjugate`** - Compute the conjugate of a quaternion (inverse for unit quat

**`quat_misori_deg`** - Calculate the misorientation angle between two quaternions i

**`quat_mult`** - Multiply two quaternions (Hamilton product).

**`quat_multiply`** - Quaternion multiplication (same as quat_mult, alternative im

**`quat_to_mat`** - Convert a quaternion to a rotation matrix.

**`rotation_from_axis_angle`** - Generate rotation matrix from axis-angle representation usin

**`simple_grid`** - Generate a uniform grid of quaternions covering orientation 

